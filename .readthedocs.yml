version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - extra
        - docs

sphinx:
  configuration: docs/source/conf.py
  builder: html
  fail_on_warning: false
